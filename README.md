# Tableau-Scribe-BETA
Auto-generate Tableau dashboard documentation and metric dictionaries from live views and datasources via MCP. Two modes: Auto-Doc (full dashboard docs from a rendered view) and Metric Builder (plain-language field definitions from a datasource). Brief or detailed depth, multiple output formats, graceful degradation on API failures.
